// write your code below!
var name = "Susan"
var height = 39
var message = `${name} to be less than ${height}`

module.exports = {name, height, message}
console.log(message)


module.exports = {name, height, message}
