// write your code below!
var name = "Susan"
var height = 39
//var message = ` ${height} to be less than 40`
var message = `${height} to be less than 40 to include ${name}`

module.exports = {name, height, message}
